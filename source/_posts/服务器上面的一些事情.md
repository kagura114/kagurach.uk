---
title: 服务器上面的一些事情
date: 2024-11-26 21:07:11
tags: [ "Linux" ]
---

对一些踩过的坑或者遇到的事情的总结，还有一些常用命令这些（也是为了方便我自己查看）
<!-- more -->
# 问题类
## Load Average 高但 CPU 占用低
可能是有某个进程卡在 I/O wait 了，下面命令列出一些可疑的进程
```bash
top -b -n 1 | awk '{if (NR <=7) print; else if ($8 == "D") {print; count++} } END {print "Total status D (I/O wait probably): "count}'
```
*但是 `kill` 对这类进程可能没用，甚至都能给你把重启卡住*

## Bitwarden vault_log.mdf 文件过大
先备份\
然后在 `./bwdata/env/mssql.override.env` 中找到你的数据库 SA 密码\
之后手动链接数据库
```bash
docker exec -it bitwarden-mssql bash
/opt/mssql-tools/bin/sqlcmd -S localhost -U SA -P '你的数据库密码'
```
在数据库中，对日志文件进行 rotate
```sql
USE vault;
GO
-- 将恢复模式更改为 SIMPLE
ALTER DATABASE vault SET RECOVERY SIMPLE;
GO
-- 收缩日志文件 (通常逻辑名为 vault_log)
DBCC SHRINKFILE (vault_log, 1);
GO
EXIT
```
之后数据库将会变为 simple 模式，日志文件大小会变小，如果你需要高可用性，可以将数据库改为 full 模式
```sql
USE vault;
GO
ALTER DATABASE vault SET RECOVERY FULL;
GO
EXIT
```

# 小技巧
## 获取默认 netdev (v6)
```bash
ip -6 route show default | head -n 1 | sed 's/.*dev \([^ ]*\).*/\1/'
```
## 快速生成一堆 ip
```bash
netdev=$(ip -6 route show default | head -n 1 | sed 's/.*dev \([^ ]*\).*/\1/')
prefix=$(ip -6 addr show dev "$netdev" scope global | awk '/inet6/ {print $2; exit}' | cut -d/ -f1 | cut -d: -f1-4)
for i in {1..2}; do
    suffix=$(dd if=/dev/urandom bs=2 count=1 2>/dev/null | od -An -tx2 | tr -d '[:space:]')
    full_ip="${prefix}::${suffix}"
    echo "Generated: $full_ip"
    sudo ip -6 addr add $full_ip/128 dev $netdev
done
```

## Simple Cloudflare DDNS

填入下方变量后脚本自动更新，点击复制即可使用：

- `ZONE_ID`：DNS 配置页面右下角可找到
- `API_TOKEN`：去 Cloudflare 创建
- `DNS_RECORD_ID`：DNS 配置页面 F12 查看请求可看到
- `DNS_NAME`：要更新的域名
- `netcard`：网络接口名

<style>
#cf-ddns-widget {
  --cf-border:       #b2ebf2;
  --cf-tag-bg:       #e1f5fe;
  --cf-tag-color:    #D56D28;
  --cf-input-bg:     #fff;
  --cf-input-color:  #444;
  --cf-input-border: #4dd0e1;
  --cf-input-focus:  #29b6f6;
  --cf-code-bg:      #faf8f4;
  --cf-code-color:   #444;
  --cf-code-border:  #b2ebf2;
  --cf-btn-bg:       #4dd0e1;
  --cf-btn-color:    #fff;
  --cf-btn-hover:    #0277bd;
}
html[data-theme="dark"] #cf-ddns-widget,
html.darkmode #cf-ddns-widget {
  --cf-border:       #4a5568;
  --cf-tag-bg:       #3a3a3a;
  --cf-tag-color:    #4dd0e1;
  --cf-input-bg:     #444;
  --cf-input-color:  #eee;
  --cf-input-border: #4a5568;
  --cf-input-focus:  #4dd0e1;
  --cf-code-bg:      #343a3c;
  --cf-code-color:   #eee;
  --cf-code-border:  #4a5568;
  --cf-btn-bg:       #4dd0e1;
  --cf-btn-color:    #222;
  --cf-btn-hover:    #b2ebf2;
}
#cf-ddns-widget table { border-collapse: collapse; width: 100%; max-width: 660px; font-size: 0.92em; }
#cf-ddns-widget td { padding: 5px 0; vertical-align: middle; }
#cf-ddns-widget td:first-child { padding-right: 14px; white-space: nowrap; width: 1%; text-align: center; }
#cf-ddns-widget code.cf-tag {
  background: var(--cf-tag-bg); color: var(--cf-tag-color);
  padding: 2px 6px; border-radius: 4px; font-family: 'JetBrains Mono', monospace; font-size: 0.95em;
}
#cf-ddns-widget input {
  width: 100%; padding: 5px 9px; box-sizing: border-box;
  background: var(--cf-input-bg); color: var(--cf-input-color);
  border: 1.5px solid var(--cf-input-border); border-radius: 5px;
  font-family: 'JetBrains Mono', monospace; font-size: 0.92em;
  outline: none; transition: border-color 0.15s;
}
#cf-ddns-widget input:focus { border-color: var(--cf-input-focus); }
#cf-ddns-code-wrap { position: relative; margin-top: 14px; }
#cf-ddns-pre {
  background: var(--cf-code-bg); color: var(--cf-code-color);
  border: 1.5px solid var(--cf-code-border);
  padding: 14px 16px; border-radius: 8px; overflow-x: auto; margin: 0;
}
#cf-ddns-code {
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.95em; line-height: 1.65; white-space: pre; display: block;
}
#cf-copy-btn {
  position: absolute; top: 8px; right: 8px;
  padding: 3px 12px;
  background: var(--cf-btn-bg); color: var(--cf-btn-color);
  border: none; border-radius: 4px; cursor: pointer;
  font-size: 0.8em; font-family: inherit; transition: background 0.15s;
}
#cf-copy-btn:hover { background: var(--cf-btn-hover); }
</style>

<div id="cf-ddns-widget">
<table>
  <tbody>
    <tr>
      <td><code class="cf-tag">ZONE_ID</code></td>
      <td><input id="cf-zone-id" type="text" placeholder="DNS 配置页面右下角" /></td>
    </tr>
    <tr>
      <td><code class="cf-tag">API_TOKEN</code></td>
      <td><input id="cf-api-token" type="text" placeholder="Cloudflare API Token" /></td>
    </tr>
    <tr>
      <td><code class="cf-tag">DNS_RECORD_ID</code></td>
      <td><input id="cf-record-id" type="text" placeholder="F12 查看 DNS 请求可找到" /></td>
    </tr>
    <tr>
      <td><code class="cf-tag">DNS_NAME</code></td>
      <td><input id="cf-dns-name" type="text" placeholder="ddns.example.com" /></td>
    </tr>
    <tr>
      <td><code class="cf-tag">netcard</code></td>
      <td><input id="cf-netcard" type="text" placeholder="eth0" /></td>
    </tr>
  </tbody>
</table>
<div id="cf-ddns-code-wrap">
  <pre id="cf-ddns-pre"><code id="cf-ddns-code"></code></pre>
  <button id="cf-copy-btn">复制</button>
</div>
</div>
<script>
(function () {
  var fields = ['cf-zone-id', 'cf-api-token', 'cf-record-id', 'cf-dns-name', 'cf-netcard'];
  var output = document.getElementById('cf-ddns-code');
  function update() {
    var zoneId   = document.getElementById('cf-zone-id').value   || 'YOUR_ZONE_ID';
    var apiToken = document.getElementById('cf-api-token').value || 'YOUR_API_TOKEN';
    var recordId = document.getElementById('cf-record-id').value || 'YOUR_DNS_RECORD_ID';
    var dnsName  = document.getElementById('cf-dns-name').value  || 'your.domain.com';
    var netcard  = document.getElementById('cf-netcard').value   || 'eth0';
    output.textContent = [
      'ZONE_ID=' + zoneId,
      'API_TOKEN=' + apiToken,
      'DNS_RECORD_ID=' + recordId,
      'DNS_NAME=' + dnsName,
      '',
      'netcard=' + netcard,
      'myip="$(',
      '    ip -6 -j addr show dev $netcard scope global primary -tentative up \\',
      "      | jq -r '(.[0] // empty).addr_info | sort_by(.prefixlen) | sort_by(.local) | map(.local // empty)[0] // empty'",
      '  )"',
      'echo "ip = ${myip}"',
      '',
      'curl https://api.cloudflare.com/client/v4/zones/$ZONE_ID/dns_records/$DNS_RECORD_ID \\',
      '    -X PUT \\',
      "    -H 'Content-Type: application/json' \\",
      '    -H "Authorization: Bearer $API_TOKEN" \\',
      '    -d "{',
      '          \\"name\\": \\"$DNS_NAME\\",',
      '          \\"ttl\\": 360,',
      '          \\"type\\": \\"AAAA\\",',
      '          \\"comment\\": \\"Updated on $(date +%c)\\",',
      '          \\"content\\": \\"${myip}\\",',
      '          \\"proxied\\": false',
      '        }"'
    ].join('\n');
  }
  fields.forEach(function (id) {
    document.getElementById(id).addEventListener('input', update);
  });
  update();
  document.getElementById('cf-copy-btn').addEventListener('click', function () {
    var btn = this;
    var text = output.textContent;
    function done() {
      btn.textContent = '✓ 已复制';
      setTimeout(function () { btn.textContent = '复制'; }, 2000);
    }
    if (navigator.clipboard) {
      navigator.clipboard.writeText(text).then(done);
    } else {
      var ta = document.createElement('textarea');
      ta.value = text; ta.style.position = 'fixed'; ta.style.opacity = '0';
      document.body.appendChild(ta); ta.focus(); ta.select();
      document.execCommand('copy'); document.body.removeChild(ta); done();
    }
  });
})();
</script>

# 更新类
## [bitwarden](https://bitwarden.com/help/updating-on-premise/)
在 bitwarden 安装目录下（例如） `/opt/bitwarden`\
按照官方教程，应该是创建了一个 `bitwarden` 用户\
则
```bash
sudo -u bitwarden ./bitwarden.sh updateself
sudo -u bitwarden ./bitwarden.sh update
```

# 备份类
## [bitwarden](https://bitwarden.com/help/backup-on-premise/)
bitwarden 最重要的是 `bwdata` 目录里的内容，就在安装目录下（例如 `/opt/bitwarden`）\
在要生成备份的地方运行下面的命令，后面是你 `bwdata` 的位置
```bash
tar cvf bitwarden-backup.tar /opt/bitwarden/bwdata/
```

## [forgejo](https://forgejo.org/docs/latest/admin/upgrade/#backup)
按照官方教程，forgejo 应该是 `git` 用户在运行，则用下面的命令 dump 数据库和存储库\
后面的是你 forgejo 的配置文件位置
```bash
sudo -u git forgejo dump --config /etc/forgejo/app.ini
```